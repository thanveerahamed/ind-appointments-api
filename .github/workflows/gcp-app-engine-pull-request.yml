# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Build CI on PR
'on': pull_request
jobs:
  build:
    name: GAE CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run gcp-build
